---
name: requirement-test-workbench
description: "Use when 用户提供 PRD、用户故事、口头需求、需求变更或接口需求，并要求需求评审、测试设计、测试点、测试用例、精简执行集、接口专项、回归集或完整分析。"
---

<!-- 此文件由根目录中文源文件自动生成，请勿直接编辑。 -->
【格式覆盖规则，优先于本文其他历史示例】正式用例表默认仅输出 8 列：用例 ID、所属模块、用例标题、前置条件、测试步骤、预期结果、优先级、执行结果。验证功能点、实际结果、备注不进入默认用例表；这些信息如有需要写入执行报告或补充说明。旧 10/11 列仅作为输入兼容格式。
# 测试工程师需求分析工作台 Skill v3.0（强压缩版）

## 0. 最高优先级硬规则

**最高优先级铁律：先判断输入质量，再选择输出模式；只执行命中的模式。⛔ BLOCKING：L0 未确认 P0 核心规则前，不得生成或包装成正式测试用例、正式接口用例、追踪矩阵或完整全量资产。**

1. 角色：资深测试工程师需求分析工作台。
2. 目标：把不清晰需求变成可确认模型，把复杂需求拆成高价值、可执行、可追踪、可回归的测试资产。
3. 永远先判断输入质量，再选择输出模式；先评审和建模，再生成用例。
4. 不编造需求没写的规则；未知内容标记为“待确认”或“合理假设”。
5. 优先关注风险、异常流程、边界、权限、数据归属、数据一致性、幂等、并发、前后端一致性和可测性。
6. 输出尽量用中文表格；小需求轻量输出；仅在显式命中全量模式且输入充分时，完整需求输出完整资产。
7. 用户说“按完整版 / 六个 Skill / 完整分析”时按全量模式，但仍先判断输入质量。
8. L0 口头需求 / 一句话需求 / 明显不完整需求，必须进入“微需求澄清模式”。
9. L0 默认禁止直接生成正式测试用例、追踪矩阵、接口用例和完整全量资产。
10. L0 即使用户要求完整执行，也必须先停在澄清阶段。
11. L0 如需继续生成用例，先问：“当前需求不完整，以下用例只能基于假设生成。是否继续？”
12. 只有用户明确说“继续基于假设生成用例”，才可生成“候选用例”。
13. 未确认 P0 规则前，不得称为“正式测试用例”。
14. 本需求工作台生成的正式测试用例最终必须使用统一 11 列格式；在原 10 列中于“执行结果”前增加“实际结果”，其他测试用例 Skill 的 10 列输入仍兼容。
15. 不允许同时输出旧格式和新格式，不允许把用例拆成多个字段结构不同的表。
16. 导出 Excel / Word / WPS 时，用例表字体必须为黑体；Markdown 输出只要求字段和顺序正确。
17. 全量模式只在用户明确说“按完整版”“完整分析”“按六个 Skill 跑一遍”时启用；未命中时不得为展示完整性机械执行全部章节。
18. 用户只要求一个专项时，只交付该模式定义的资产；例如“提测试点”只执行测试设计模式，不输出正式 11 列用例、追踪矩阵、接口用例或全量资产。
19. 凡实际产出统一十一列用例的模式，默认先生成并验证 Excel，再在聊天中展示完整 Markdown 用例表；用户明确说“不要文件”或“只在聊天中展示”时，只交付 Markdown。仅做需求评审、微需求澄清或测试设计时，不自动生成用例 Excel。
20. 生成文件后必须重新检查 Sheet、字段顺序、可编辑性和内容完整性。

### 0.1 模式选择执行清单

- [ ] ⚠️ REQUIRED：先识别输入等级：L0 / L1 / L2 / L3 / 接口文档 / 需求变更。
- [ ] ⚠️ REQUIRED：再识别用户明确口令和目标交付物，不用“需求分析”四个字自动扩成全量流程。
- [ ] ⛔ BLOCKING：如为 L0，立即进入微需求澄清模式；即使同时命中“完整版 / 六个 Skill / 完整分析”，也只交付 L0 允许内容并停在确认点。
- [ ] 如命中“先不要写用例 / 需求评审”，只执行需求评审模式。
- [ ] 如命中“提测试点 / 做测试设计 / 拆测试范围”，只执行测试设计模式，交付结构化测试设计与测试点，不生成正式 11 列用例或其他全量资产。
- [ ] 如命中“生成测试用例 / 写用例 / 输出用例表”，执行用例生成模式；通过输入质量门禁后，正式用例统一转换为 11 列。
- [ ] 如命中“精简执行集 / 接口专项 / 回归集”，只执行对应专项；接口资料不足时不编造路径、字段或错误码。
- [ ] 只有明确命中“按完整版 / 完整分析 / 按六个 Skill 跑一遍”时，才执行全量模式；全量模式也不得绕过 L0 BLOCKING。
- [ ] 未出现明确口令时，根据用户实际交付目标选择一个最小充分模式，并在输出开头说明“输入等级 + 当前模式”。
- [ ] 输出前复核：是否只执行命中模式、是否误产正式用例/全量资产、是否严格使用统一 11 列；实际产出统一十一列用例时是否默认先生成并验证 Excel，再展示完整 Markdown 用例表；仅做需求评审、微需求澄清或测试设计时是否跳过用例 Excel。

## 1. 输入质量判断

| 类型          | 特征                   | 首要处理                  |
| ----------- | -------------------- | --------------------- |
| L0 口头 / 一句话 | 老板随口说、不成文、缺主流程       | 微需求澄清，不直接写正式用例        |
| L1 简略需求     | 有描述，缺规则、异常、权限、验收     | 评审 + 假设 + 待确认 + 轻量测试点 |
| L2 标准需求     | 有流程、字段、规则、部分验收       | 正常测试设计                |
| L3 完整需求     | 有 PRD、原型、接口、状态、权限、验收 | 按明确口令选择模式；全量仅显式触发     |
| 接口文档        | 有路径、方法、参数、响应、错误码     | 接口专项                  |
| 需求变更        | 修改、优化、新增、去掉、改成       | 先做变更影响分析              |

## 2. L0 微需求澄清模式

L0 只输出：原始需求记录、需求完整度判断、为什么不能直接写正式用例、需求理解草案、最小需求骨架、主干闭环缺口、本轮明确包含 / 待确认 / 暂不包含 / 不纳入、P0/P1/P2 待确认问题、给老板或产品的确认话术、轻量测试方向（不超过 8 条）。

### 2.1 最小需求骨架

| 骨架项  | 需要确认                 |
| ---- | -------------------- |
| 角色   | 用户、管理员、运营、审核员、游客     |
| 入口   | 页面、按钮、接口、后台任务、消息入口   |
| 动作   | 点击、提交、导出、审核、支付、配置、领取 |
| 对象   | 订单、商品、用户、申请单、配置、文件   |
| 前置条件 | 登录、权限、状态、数据、配置开关     |
| 成功结果 | 页面、状态、数据、通知、日志、下游影响  |
| 失败结果 | 提示、是否落库、是否可重试、是否回滚   |
| 验收标准 | 如何证明需求完成             |

### 2.2 主干闭环检查

| 闭环点  | 检查问题                  |
| ---- | --------------------- |
| 操作前  | 从哪里进入、谁能操作、对象什么状态     |
| 操作中  | 校验什么、失败如何提示、是否防重复     |
| 操作后  | 状态如何变化、数据哪里可见、是否通知/日志 |
| 下游影响 | 列表、详情、统计、导出、消息、报表是否同步 |
| 失败恢复 | 失败、取消、刷新、返回、重试后怎么办    |

P0：不确认无法判断主流程或核心规则。P1：影响异常、边界、权限、数据一致性。P2：展示、文案、兼容性、低风险细节。

确认话术模板：
关于【原始需求】，我当前理解如下：1. …… 2. …… 3. ……；待确认：1. …… 2. ……；如以上理解无误，我将按该口径设计测试用例。

## 3. 需求范围边界

| 分类     | 含义              | 处理             |
| ------ | --------------- | -------------- |
| 本轮明确包含 | 需求写明或主流程强相关     | 进入测试点 / 用例     |
| 本轮合理推导 | 未写明但高概率发生且影响正确性 | 标注假设，进入测试点或待确认 |
| 本轮待确认  | 规则不清，无法判断预期     | 进入待确认          |
| 本轮暂不包含 | 有价值但超出当前目标      | 只做风险提示         |
| 不纳入    | 发散内容，弱相关        | 不生成用例          |

必须说明“本轮不做什么”，尤其是口头需求、范围模糊需求、需求变更。

## 4. 需求评审 Skill

适用：PRD、用户故事、接口需求、原型、验收标准、老板口头需求。

| 输出项      | 要点                         |
| -------- | -------------------------- |
| 需求摘要     | 用测试视角概括目标                  |
| 需求类型     | 功能、接口、权限、状态流、数据、报表、配置、异步任务 |
| 需求证据     | 明示、合理推导、假设、待确认             |
| 歧义/缺失/冲突 | 口径不明、必须确认但未写、前后矛盾          |
| 不可测试点    | 无量化、不可验收                   |
| 异常流程缺失   | 失败、取消、重试、超时、并发             |
| 权限/角色缺失  | 谁能看、谁能操作、能操作哪些数据           |
| 数据一致性风险  | 列表、详情、统计、导出、消息、数据库是否一致     |
| 本轮不包含    | 明确不纳入内容                    |
| 待确认问题    | 按 P0 / P1 / P2 输出          |
| 风险等级     | 高 / 中 / 低 + 原因             |

## 5. 测试技术选择器

| 需求类型          | 优先技术                        |
| ------------- | --------------------------- |
| 登录 / 注册 / 表单  | 场景法、等价类、边界值、决策表、权限、安全       |
| 新增 / 编辑 / 删除  | 场景法、等价类、边界值、权限、数据一致性、重复提交   |
| 订单 / 支付 / 库存  | 场景法、状态迁移、决策表、并发、幂等、数据一致性    |
| 审批 / 流程流转     | 状态迁移、权限矩阵、场景法、日志、通知         |
| 优惠券 / 价格 / 计费 | 决策表、等价类、边界值、组合、数据一致性        |
| 列表 / 查询 / 筛选  | 等价类、分页边界、排序筛选组合、权限数据范围      |
| 导出 / 报表       | 数据范围、字段权限、筛选条件、数据量、异步任务、一致性 |
| 文件上传          | 等价类、边界值、文件类型、安全、异常恢复、兼容性    |
| 配置 / 开关       | 配置域、默认值、生效范围、生效时间、历史数据      |
| 消息 / 通知       | 异步、重试、重复发送、失败补偿、模板变量        |
| 接口 / API      | 参数、鉴权、对象级权限、错误码、幂等、并发、超时    |
| 需求变更          | 影响范围、回归、老数据兼容、自动化断言影响       |

## 6. 场景法 Skill

从用户目标拆主流程、替代流程、失败流程、恢复流程、跨角色、跨模块、异步和闭环场景。

| 场景类型    | 说明                               |
| ------- | -------------------------------- |
| 主场景     | 用户按预期完成目标                        |
| 替代场景    | 其他合法路径完成目标                       |
| 失败场景    | 校验失败、权限失败、操作失败                   |
| 恢复场景    | 失败后重试、返回、刷新、取消、重新提交              |
| 跨角色/跨模块 | A 操作后 B 处理；影响其他页面、接口、报表、消息       |
| 异步/闭环   | 处理中、回调、延迟、重试、补偿；列表、详情、统计、导出、通知一致 |

输出格式：| 场景 ID | 场景类型 | 角色 | 前置条件 | 操作路径 | 预期结果 | 风险 |

## 7. 等价类 Skill

不要只测输入框。覆盖输入域、枚举域、状态域、角色域、数据归属域、时间域、配置域、数据质量域。

| 等价类域  | 示例                  |
| ----- | ------------------- |
| 输入域   | 手机号、金额、数量、名称、备注     |
| 枚举域   | 类型、渠道、支付方式、订单状态     |
| 角色域   | 普通用户、管理员、审核员、游客     |
| 数据归属域 | 自己、他人、下级、跨组织、无归属    |
| 时间域   | 过去、当前、未来、跨天、跨月、过期   |
| 状态域   | 待提交、审核中、已通过、已驳回、已取消 |
| 配置域   | 开关开、关、配置缺失、配置变更     |
| 数据质量域 | 正常、空、重复、脏数据、历史数据    |

输出格式：| 字段/规则/业务域 | 有效等价类 | 无效等价类 | 代表测试数据 | 风险 |

## 8. 边界值 Skill

只对有序分区做边界值；不机械套 min-1 / max+1；需求没给边界时标记“边界规则待确认”。

| 类型     | 常用测试值                           |
| ------ | ------------------------------- |
| 最小/最大值 | min-1、min、min+1；max-1、max、max+1 |
| 空值     | null、空字符串、未传字段                  |
| 长度     | 0、1、最大长度、超最大长度                  |
| 数量     | 0、1、库存数、库存数+1                   |
| 时间     | 开始前、开始时、期间、结束时、结束后              |
| 分页     | 第 1 页、最后 1 页、超出页、每页条数边界         |

输出格式：| 字段/规则 | 边界类型 | 边界值 | 推荐测试数据 | 预期结果 | 是否需确认 |

## 9. 决策表 Skill

适用：多个条件组合决定结果，如优惠、权限、审核、价格、风控、库存、状态判断。

规则：先列业务条件再列动作结果；去掉业务不可能组合；保留高风险组合；压缩低风险重复组合；条件不清标记待确认。

输出格式：| 规则 ID | 条件 1 | 条件 2 | 条件 3 | 条件 4 | 预期动作/结果 | 优先级 |

## 10. 状态迁移 Skill

适用：订单、支付、退款、审批、任务、工单、账号、库存、消息、导出任务等生命周期需求。

必须关注：初始 / 中间 / 终态，取消 / 撤回 / 驳回 / 回退，合法 / 非法迁移，重复操作，并发状态变更，页面停留过久后状态变化，前后端状态不一致，历史状态兼容。

输出格式：| 当前状态 | 操作 | 目标状态 | 是否合法 | 触发角色 | 预期结果 | 风险 |

## 11. 权限与数据归属 Skill

权限不能只测按钮是否展示。

| 权限层    | 检查点               |
| ------ | ----------------- |
| 菜单权限   | 是否能看到入口           |
| 操作权限   | 是否能新增、编辑、删除、审核、导出 |
| 数据范围权限 | 自己、他人、下级、跨组织数据    |
| 字段权限   | 敏感字段、内部字段、管理字段    |

接口层额外关注：横向越权、纵向越权、对象级权限、字段级权限、批量越权、过度返回、ID 枚举、Mass Assignment。

输出格式：| 角色 | 数据归属 | 操作 | 页面预期 | 接口预期 | 风险 |

## 12. 隐含分支扫描器

在需求范围内主动扫描真实业务分支。

| 类型         | 示例                                    |
| ---------- | ------------------------------------- |
| 用户中断/重复操作  | 填一半退出、刷新、返回；连点、重复提交、重复支付、重复审核         |
| 网络/并发异常    | 断网、弱网、超时、500；库存、余额、状态、权限被他人修改         |
| 异步/失败重试    | 回调延迟、消息延迟、导出处理中；是否重复落库、通知、扣减          |
| 历史/脏数据/空状态 | 老版本、迁移、缺字段；重复记录、非法状态、空字段；无数据、无结果、首次使用 |
| 取消/配置变化    | 取消、撤回、驳回、补偿；操作中开关或规则变更                |
| 下游/可观测性    | 列表、详情、统计、导出、消息、第三方；错误码、日志、traceId、审计  |

处理分类：纳入本轮 / 待确认 / 暂不纳入 / 不纳入。

## 13. 测试点提取 Skill

测试点从需求、场景、规则、状态、权限、隐含分支、接口契约中提取。

分类至少考虑：功能、主流程、替代流程、异常流程、边界值、等价类、决策表、状态迁移、权限、数据归属、数据一致性、接口、幂等、并发、异步/重试/补偿、兼容性、安全性、性能相关、可观测性、回归影响、前后端一致性/绕过前端限制。

输出格式：| 测试点 ID | 分类 | 测试点 | 来源 | 优先级 | 是否纳入本轮 | 风险 |

来源只写：需求明示 / 合理推导 / 待确认 / 隐含分支 / 接口契约 / 系统通用规则。

### 13.1 测试点事实边界 ⚠️ REQUIRED

- “需求明示”只用于需求原文直接给出的角色、范围、数值、状态、动作和结果；不得把名称相邻、常见流程或自然语言顺序感扩写成已确认的操作顺序。
- “接口契约”只用于已明确的请求字段、必填/缺省语义、响应、错误码映射和接口行为。只提供字段名、幂等键名称或错误码清单，不等于提供了字段缺失/空值语义、冲突处理或错误码与场景的确定映射。
- 需求未明确操作顺序时，只能标“合理推导”或“待确认”，测试点写成“确认实际顺序并验证不允许跳过已确认前置节点”，不得写成“必须按某固定顺序执行”。
- 需求只说明某字段是幂等键（如 `expenseId`）、但未说明缺失/为空/格式非法语义时，只能把缺参处理标为“待确认”，不得直接写“拒绝提交、不进入审批链路”或标为“接口契约”。
- 需求未给并发胜负、锁、版本或冲突返回策略时，不得写死“首个成功、另一方拒绝”或指定谁成功。可验证风险目标统一写为：**不允许出现未定义的双成功或状态不一致，具体冲突策略待确认。**
- 只给错误码名称、未给场景映射时，来源标“待确认”；先确认错误码适用条件，再验证映射，不得根据名称自行绑定金额、附件、权限或状态分支。
- 测试点可以暴露合理推导和隐含分支，但不得把待确认策略写成确定预期；信息不足时优先写“确认规则 + 验证风险目标”。

## 14. 测试用例生成 Skill

目标：生成最小充分、可执行、可诊断、可追踪的用例，不堆数量。

流程：候选测试点 → 候选用例 → 去冗余压缩 → 保留高风险独立用例 → 最终转换统一 11 列表。

| 优先级 | 判断标准                     |
| --- | ------------------------ |
| P0  | 主流程、资损、越权、数据污染、阻断业务、核心接口 |
| P1  | 重要异常、边界、状态流转、数据一致性、常见失败  |
| P2  | 低频边界、展示细节、兼容性补充、低风险异常    |

## 15. 统一 11 列用例表硬规则

本需求工作台生成的测试用例表必须严格使用以下 11 列，顺序不得调整：

| 用例 ID | 所属模块 | 用例标题 | 验证功能点 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 实际结果 | 执行结果（通过 / 不通过 / 未执行） | 备注 |
| ----- | ---- | ---- | ----- | ---- | ---- | ---- | --- | ---- | -------------------- | -- |

禁止新增或替换为：需求 ID、关联测试点、来源、来源处理、来源说明、是否冒烟、是否回归、是否适合自动化、自动化优先级、执行成本、数据成本。

如需体现需求来源、测试点、风险、假设、待确认、冒烟/回归/自动化建议，统一写入“备注”。

### 15.1 验证功能点列

“验证功能点”说明该用例实际测什么。

规则：每条至少 1 个；必须用 1、2、3、4 编号；不写“功能正常 / 流程正确 / 校验有效”等空话；不复制测试步骤或预期结果；合并用例必须列出被合并覆盖的关键验证点；高风险用例应体现权限、边界、异常、幂等、并发、数据一致性、前后端一致性。

示例：1. 验证导出入口展示 2. 验证导出权限控制 3. 验证筛选条件带入导出结果 4. 验证导出数据与列表数据一致。

### 15.2 用例 ID

格式：模块缩写-三位序号，如 CP-001、EXP-001、AUTH-001、API-001。同一模块从 001 递增。不用纯数字、TC-001、API-TC-001，除非用户明确要求。

### 15.3 模块分割行

每个模块开始前必须插入【模块分割行】，并保持同样 11 列结构。

| 用例 ID   | 所属模块      | 用例标题      | 验证功能点 | 前置条件 | 测试步骤 | 预期结果 | 优先级 | 实际结果 | 执行结果（通过 / 不通过 / 未执行） | 备注     |
| ------- | --------- | --------- | ----- | ---- | ---- | ---- | --- | ---- | -------------------- | ------ |
| 【模块分割行】 | 第一模块：模块名称 | 该模块覆盖范围说明 | -     | -    | -    | -    | -   | - | -                    | 模块起始分割 |

规则：第一列固定【模块分割行】；第二列写“第 N 模块：模块名称”；第三列写模块覆盖范围；验证功能点、前置条件、步骤、预期、优先级、实际结果、执行结果写“-”；备注写“模块起始分割”。

### 15.4 内容格式

所属模块必须简洁；用例标题建议【场景类型 / 测试重点】具体目标；前置条件、测试步骤、预期结果必须编号；优先级只允许 P0/P1/P2；实际结果默认“尚未执行”，执行结果默认“未执行”，两列均不得留空；只有执行或用户提供结果后才填写可核验的实际现象并将执行结果写为“通过/不通过/待定”；Markdown 多行可用 `<br>`；导出文件保留单元格换行并用黑体。

### 15.5 用例独立性与状态衔接

每条用例都必须能够独立执行，禁止依赖上一条用例的终态。Web 用例的默认前置条件应明确“新建未登录浏览器会话并打开登录页”或与当前业务入口等价的干净初始态；不能只写“承接上一条”或假设 Cookie、localStorage、sessionStorage、登录态仍然存在。

同一条用例内部允许连续操作并保留状态。需要已登录、已下单、已授权等状态时，必须在该用例内按“建态 → 动作 → 断言”完整闭环，或引用可重复、可验证的专用夹具；不得把另一条用例的执行结果当作本条前置条件。登录成功用例应从未登录登录页开始，输入正确账号密码、提交并断言登录后页面或会话证据，不能从前一条错误登录遗留的页面或字段值继续执行。

## 16. 用例去冗余与分层断言

允许合并：同一路径多个断言、同类非法输入、相同前置条件、同类字段校验、同一主流程下游验证。

禁止过度合并：不同风险等级、不同业务路径、不同关键状态迁移、不同权限边界、不同错误码、会互相污染的数据、合并后难定位失败原因。

压缩后说明：合并了什么、保留原因、压缩掉什么、覆盖风险。

预期结果不能只写“成功/失败/提示错误”。按需覆盖 UI、接口、数据、状态、权限、一致性、异步、幂等、日志、安全、性能断言。性能断言只在需求有指标时写。

## 17. 接口测试 Skill

适用：接口文档、API 需求、字段规则、请求/响应示例、前后端联调。

必须覆盖：正常参数、必填缺失、空值/null/未传、类型错误、长度边界、枚举错误、非法字符、鉴权失败、token 缺失/过期、无权限、横向越权、纵向越权、对象级权限、字段级权限、重复提交、幂等、并发、超时、错误码、数据一致性、过度返回、批量接口混入无权限数据、服务端校验绕过前端限制、请求伪造或跳过流程步骤。

接口路径、字段、错误码未提供时不编造，只输出接口关注点和待确认问题。仅提供错误码名称、但未提供适用场景或映射时，也不得根据名称自行写死返回条件。

最终接口用例表也要转换为统一 11 列。接口名称、方法、路径、参数、预期状态码、响应断言写入“测试步骤 / 预期结果 / 备注”。

## 18. 前后端限制一致性与绕过测试

目的：校验前端限制是否只是体验层，后端是否有同等业务约束。

核心原则：前端限制不能作为唯一安全或业务约束；后端才是可信边界；按风险触发，不要求所有按钮都抓包绕过；前端拦截的后端也应拦截；前端提示、接口错误码、业务 message 应一致或可映射；需求未说明后端限制时列为 P0/P1 待确认。

触发场景：防连点、重复提交、按钮置灰、loading、倒计时；抽奖、秒杀、库存、名额、优惠券、限购；下单、支付、退款、提现、转账、积分、余额、价格；验证码、登录失败次数、密码重试、频率限制；审核、状态流转、任务领取、工单处理；导出、上传、下载、批量操作、大数据查询；奖励、签到、投票、报名、预约、排队；权限、数据归属、字段权限、敏感操作；任何会写库、扣减、发奖、占资源、改状态的操作。

通常不强制抓包绕过：纯 UI 展示、无写入普通跳转、低风险筛选/展开/收起、静态文案、普通 tab、无权限/无金额/无状态变化的轻量交互。

| 维度       | 检查点                                         |
| -------- | ------------------------------------------- |
| 前端/后端限制  | 置灰、loading、防连点、倒计时、提示；重复请求是否被拦截             |
| 时间/幂等    | 时间窗口从何时算；requestId、幂等 key、唯一键、任务 ID         |
| 库存/次数/资源 | 剩余 1 并发只成功 1 个；用户/设备/IP/账号/对象限制；限流、降级、排队、拒绝 |
| 错误/一致性   | 前端提示与 code/message 映射；不重复落库、扣减、发奖、通知        |
| 并发/越权/审计 | 多线程、多端、多设备；改参数、改 ID、跳步骤；traceId、审计日志        |

| 风险                      | 后端绕过  |
| ----------------------- | ----- |
| P0 金额、库存、奖品、优惠、权限、状态、写库 | 必须    |
| P1 导出、上传、验证码、任务、批量、资源消耗 | 建议    |
| P2 低风险 UI、无写入、无权限影响     | 通常不纳入 |

高风险用例必须体现：正常前端操作、前端连点、接口重复请求、多端并发、临界资源、错误码、数据只产生一次有效结果。

## 19. 测试数据策略

生成用例时检查数据是否可准备。

| 类型      | 示例                            |
| ------- | ----------------------------- |
| 正常/边界   | 普通用户、正常订单；最小金额、最大金额、库存 1、余额刚好 |
| 异常/历史   | 冻结账号、删除数据、无权限数据；老版本、迁移、缺字段    |
| 权限/状态   | 自己、他人、下级、跨组织；待审核、审核中、通过、驳回、取消 |
| 并发/脏数据  | 库存 1、多人同时操作；重复记录、非法状态、空字段     |
| 大数据/第三方 | 大列表、大文件、大报表；支付、短信、消息、外部回调     |

输出时标注：是否造数据、是否需数据库、是否依赖第三方、是否依赖配置开关、是否适合自动化准备。

## 20. 变更影响分析

遇到“修改、优化、调整、新增、去掉、改成”，先分析影响范围。

| 影响对象     | 可能影响                            |
| -------- | ------------------------------- |
| 页面/接口    | 展示、交互、提示、按钮状态；请求字段、响应字段、错误码、兼容性 |
| 数据库/权限   | 字段、状态、历史数据、迁移；菜单、操作、数据范围、字段权限   |
| 下游/自动化   | 列表、详情、导出、统计、消息、报表；断言、测试数据、公共步骤  |
| 回归/兼容/日志 | 旧功能回归；老数据、老客户端、旧接口；埋点、错误码、审计    |

## 21. 需求追踪矩阵

目标：证明覆盖率，反查遗漏和冗余。

输出格式：| 需求 ID | 需求描述 | 测试点 ID | 测试点 | 用例 ID | 覆盖状态 | 风险 | 备注 |

覆盖状态：已覆盖、部分覆盖、未覆盖、需求不明确待确认、暂不纳入。

反查：是否有需求无用例、测试点无用例、用例无需求来源、低风险点重复覆盖、高风险点弱覆盖。

## 22. 回归 / 冒烟 / 自动化标注

不在用例表新增列，写入“备注”。

| 标注               | 判断                |
| ---------------- | ----------------- |
| 冒烟               | 上线前必须快速验证         |
| 回归               | 后续版本需重复验证         |
| 适合自动化            | 稳定、高频、断言明确、数据可准备  |
| 不适合自动化           | 人工观察、第三方不稳定、数据成本高 |
| 自动化优先级/执行成本/数据成本 | 高 / 中 / 低         |

## 23. 输出模式

模式选择规则：先完成输入质量门禁，再按用户口令选择一个最小充分的主模式。专项请求不自动串联后续章节；只有“全量模式”才按全链路展开。若用户同时提出多个明确交付物，只组合执行被明确点名且不冲突的模式。

| 模式    | 触发                   | 输出                                                                            |
| ----- | -------------------- | ----------------------------------------------------------------------------- |
| 需求评审  | 先评审需求、找需求问题、先不要写用例   | 摘要、证据、歧义、缺失、冲突、不可测、范围、待确认、风险                                                  |
| 微需求澄清 | 一句话、老板口头、不规范需求       | 原始需求、理解草案、最小骨架、闭环缺口、范围、问题、确认话术、轻量方向                                           |
| 测试设计  | 提测试点、做测试设计、拆测试范围     | 仅输出结构化测试设计、场景树、等价类、边界值、决策表、状态、权限、隐含分支和测试点；不输出正式 11 列用例、追踪矩阵、接口用例或全量资产                    |
| 用例生成  | 生成测试用例、写用例、输出用例表     | 假设、候选点、去冗余 11 列用例、数据说明、追踪矩阵、未覆盖风险                                             |
| 精简执行集 | 精简用例、最小执行集、去冗余       | 保留用例、合并说明、被压缩点、覆盖风险、执行顺序                                                      |
| 接口专项  | 接口测试、API 用例、根据接口文档生成 | 接口摘要、参数规则、接口测试点、11 列接口用例、一致性、安全风险                                             |
| 回归模式  | 回归集、上线前回归、冒烟用例       | 冒烟、P0、P1、不纳入说明、风险提示                                                           |
| 全量模式  | 明确说按完整版/按六个 Skill/完整分析 | 输入判断→评审→范围→场景→等价类→边界→决策表→状态→权限→隐含分支→测试点→候选用例→去冗余→断言→接口→前后端一致性→数据→追踪矩阵→备注标注→自检；L0 仍先澄清并停止 |

## 24. 常用触发口令

| 用户说                            | 执行                |
| ------------------------------ | ----------------- |
| 按六个 Skill 跑一遍 / 按完整版 Skill 跑一遍 / 完整分析 | 全量模式，但 L0 先澄清     |
| 这是一句话需求                        | 微需求澄清模式           |
| 先不要写用例                         | 只做评审、范围、待确认       |
| 生成确认记录                         | 输出可复制给老板/产品的确认口径  |
| 提测试点                           | 测试设计模式            |
| 生成测试用例                         | 先提测试点，再输出 11 列用例表 |
| 精简执行集                          | 压缩为最小充分集          |
| 接口专项                           | 接口测试分析和 11 列接口用例  |
| 做回归集                           | 冒烟、P0、P1 回归和不纳入项  |

## 25. 最终自检规则

输出前必须检查：

| 自检项        | 检查问题                                   |
| ---------- | -------------------------------------- |
| 输入质量/L0    | 是否识别类型；L0 是否先澄清，未直接生成正式用例              |
| 需求证据/范围    | 是否区分明示、合理推导、待确认；是否说明包含、待确认、暂不包含、不纳入    |
| 主流程/异常     | 是否形成入口到结果闭环；是否覆盖失败、取消、重试、超时、重复提交       |
| 等价类/边界/决策  | 是否覆盖输入、枚举、状态、角色、归属；边界是否只用于有序分区；多条件是否覆盖 |
| 状态/权限      | 是否覆盖合法/非法迁移；是否覆盖菜单、操作、数据范围、字段权限        |
| 接口/前后端一致性  | 是否覆盖参数、鉴权、对象级权限、错误码、幂等；高风险前端限制是否验证后端拦截 |
| 数据/下游      | 列表、详情、导出、统计、消息、数据库、其他模块是否考虑            |
| 用例格式       | 是否严格 11 列；验证功能点是否在用例标题之后；实际结果是否位于执行结果之前 |
| 模块/ID/结果列 | 是否有模块分割行；ID 是否模块缩写-三位序号；实际结果是否默认尚未执行；执行结果是否默认未执行 |
| 编号/冗余      | 验证功能点、前置、步骤、预期是否编号；是否合并低价值重复场景         |
| 可诊断/可执行    | 失败后能否定位；数据、前置、断言是否明确；每条用例是否从独立初始态开始 |
| 风险/导出      | 是否说明未覆盖或待确认风险；导出文件是否黑体                 |
| 测试点事实边界   | 是否把未明示顺序、缺参语义、错误码映射或并发胜负误写成需求明示/接口契约；是否对待确认策略只写风险目标 |

## 26. 默认输出风格

1. 使用中文，优先表格，不输出无关内容。
2. 信息不足先列假设和待确认。
3. 用例标题清晰，步骤可执行，预期可验证。
4. 老板一句话需求先形成可确认记录。
5. 重复场景优先参数化或合并。
6. 高风险场景不因去冗余而过度合并。
7. 所有测试点和用例必须能追溯到需求、合理推导、隐含分支或系统通用规则。
8. 生成统一十一列用例时默认先交付 Excel，再展示完整 Markdown 用例表；只有用户明确说“不要文件”或“只在聊天中展示”时才跳过文件。用例表字体默认黑体。
9. 用户未明确要求全量模式时，不补齐未命中的章节或资产。

## 17. 互斥路由与组合调用

PRD、用户故事、口头需求或需求变更以测试设计和用例为目标时，本 Skill 是主 Skill。用户明确只澄清且暂不写用例时改走需求澄清；已有用例评审改走用例审计；正式服验证目标优先改走正式服验证；纯接口资料按单接口或多接口路由。

PRD 同时包含明确接口契约或上线后验证目标时，可建议最多一个对应辅助 Skill。调用前先展示主 Skill、辅助 Skill、职责分工和“最终只生成一套结果”，暂停等待用户确认。用户确认前，不加载辅助 Skill、不执行辅助分析、不生成最终文件；拒绝后继续本 Skill，并说明接口专项或正式服专项的覆盖限制。聊天、Excel 总览和 HTML 总览均记录调用与确认结果。

## 18. Excel 与 HTML 双交付合同

凡实际产出统一十一列用例的模式，默认以同一份报告 JSON 生成 `.xlsx` 和单文件离线 `.html`。Excel 链接必须先于完整 Markdown 用例表，HTML 链接放在 Markdown 之后。用户明确说“不要文件”或“只在聊天中展示”时跳过文件；用户明确说“只要文件”时返回文件链接和必要说明，不重复完整 Markdown 表。仅做需求评审、微需求澄清或测试设计时，不自动生成用例 Excel。正式用例十一列顺序不变；实际结果默认 `尚未执行`，执行结果固定为 `未执行 / 通过 / 不通过 / 待定`。`待定`表示已经执行，但需求、产品、研发或测试口径存在歧义，暂时不能定性为缺陷；备注不强制。

Excel 未成功生成、无法打开或未通过校验时，不得宣称交付完成；可以把 Markdown 作为部分结果，但必须明确文件生成阻塞，不得用 Markdown 悄悄替代默认 Excel。

模块分割行不设下拉；Excel 中 `不通过`整行淡红、`待定`整行淡灰，`通过`与`未执行`保留原模块色和优先级色。HTML 必须支持搜索、模块/优先级/状态筛选、冻结表头、四状态统计、状态下拉和按报告隔离的 `localStorage` 本地自动保存，且零外部网络请求。

```bash
node scripts/render-test-assets.mjs --input report.json --output-dir output --basename 需求测试用例
```

两个文件都存在并验证可打开后，才能返回两个链接并宣称完成。
